############################################################################
# VelaGesture — Make.defs
############################################################################

ifneq ($(CONFIG_VELAGESTURE),)
CONFIGURED_APPS += $(APPDIR)/packages/demos/contest2026_439_velagesture
endif
